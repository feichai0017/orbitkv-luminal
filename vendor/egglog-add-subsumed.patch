--- /Users/austin/.cargo/git/checkouts/egglog-7c9491f597880af0/c2c0f15/src/exec_state.rs	2026-08-14 13:58:23
+++ egglog-patched/src/exec_state.rs	2026-08-14 17:32:32
@@ -593,6 +593,23 @@
         Ok(value)
     }
 
+    /// Look up or insert a constructor row with its subsume flag SET —
+    /// the row is born retired (no one-round live window). Existing
+    /// staged rows merge sticky (max), so a same-action live insert of
+    /// the identical row stays retired; callers who must preserve
+    /// committed live rows should check [`Read::contains`] first.
+    /// (LOCAL PATCH — proposed upstream alongside egglog-experimental #60.)
+    fn add_subsumed<K: IntoValues>(&mut self, name: &str, key: K) -> Result<Value, Error> {
+        let action = lookup_action(self.registry(), name)?;
+        check_subtype(name, &action, TableKind::Constructor)?;
+        let key_values: ValueRow = key.into_values(self.base_values()).collect();
+        check_arity(name, &action, key_values.len())?;
+        let value = action
+            .lookup_or_insert_subsumed(self.es_mut(), &key_values)
+            .expect("constructor lookup_or_insert_subsumed returned None");
+        Ok(value)
+    }
+
     /// Remove a row from the named table. Works for any subtype.
     fn remove<K: IntoValues>(&mut self, name: &str, key: K) -> Result<(), Error> {
         let action = lookup_action(self.registry(), name)?;
--- /Users/austin/.cargo/git/checkouts/egglog-7c9491f597880af0/c2c0f15/egglog-bridge/src/lib.rs	2026-08-14 13:58:23
+++ egglog-patched/egglog-bridge/src/lib.rs	2026-08-14 17:32:32
@@ -1549,6 +1549,47 @@
         }
     }
 
+    /// Look up a row, inserting it with its subsume flag SET if absent —
+    /// the row is born retired, with no live window. Written for the
+    /// substitution walk: the copy of an already-subsumed source row must
+    /// never become matchable material. The subsume column merges by max
+    /// (sticky), so calling this on a row staged live in the same action
+    /// retires it; committed rows should be checked by the caller first.
+    /// (LOCAL PATCH — proposed upstream alongside egglog-experimental #60.)
+    pub fn lookup_or_insert_subsumed(
+        &self,
+        state: &mut ExecutionState,
+        key: &[Value],
+    ) -> Option<Value> {
+        match self.default {
+            Some(default) => {
+                let timestamp =
+                    MergeVal::Constant(Value::from_usize(state.read_counter(self.timestamp)));
+                let mut merge_vals = SmallVec::<[MergeVal; 3]>::new();
+                SchemaMath {
+                    func_cols: 1,
+                    ..self.table_math
+                }
+                .write_table_row(
+                    &mut merge_vals,
+                    RowVals {
+                        timestamp,
+                        subsume: self
+                            .table_math
+                            .subsume
+                            .then_some(MergeVal::Constant(SUBSUMED)),
+                        ret_val: Some(default),
+                    },
+                );
+                Some(
+                    state.predict_val(self.table, key, merge_vals.iter().copied())
+                        [self.table_math.ret_val_col()],
+                )
+            }
+            None => self.lookup(state, key),
+        }
+    }
+
     /// Insert a row into this table.
     pub fn insert(&self, state: &mut ExecutionState, row: impl Iterator<Item = Value>) {
         let ts = Value::from_usize(state.read_counter(self.timestamp));
