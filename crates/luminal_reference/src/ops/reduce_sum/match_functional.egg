; NON-INT dtypes: ungated (float arithmetic carries its own
; semantics; the non-wrapping ruling gates Int only).
; Layout Tensor Op Reduce Sum Generic
(rule
  (
    (= ?in_layout_tensor (LayoutTensorLit ?in_logical ?in_layout))
    ; CANONICAL-ONLY: this slot must carry its logical's own right-major
    ; contiguous layout. The kernels fabricate dense right-major strides
    ; in Rust and never read a layout, so before this premise a left-major
    ; operand was accepted and silently mis-indexed.
    (= ?in_layout (RightMajorContiguousElementLayoutLit ?in_cshape ?in_cbits))
    (= ?in_cshape (shape-of ?in_logical))
    (= ?in_cbits (bits-of (dtype-of ?in_logical)))
    (= ?out_layout_tensor (LayoutTensorLit ?out_logical ?out_layout))
    ; CANONICAL-ONLY: this slot must carry its logical's own right-major
    ; contiguous layout. The kernels fabricate dense right-major strides
    ; in Rust and never read a layout, so before this premise a left-major
    ; operand was accepted and silently mis-indexed.
    (= ?out_layout (RightMajorContiguousElementLayoutLit ?out_cshape ?out_cbits))
    (= ?out_cshape (shape-of ?out_logical))
    (= ?out_cbits (bits-of (dtype-of ?out_logical)))
    (= ?out_logical (LogicalReduceSum ?in_logical ?reduction_dim))
    (= ?value_dtype (dtype-of ?out_logical))
    (!= ?value_dtype (Int))
    (!= ?value_dtype (Int64))
    (= (injectivity-of ?out_layout_tensor) (Injective))
  )
  (
    (let ?layout_tensor_op
      (LayoutTensorOpLit
        (LayoutTensorCons ?in_layout_tensor (LayoutTensorNil))
        (LayoutTensorCons ?out_layout_tensor (LayoutTensorNil))))
    (union ?layout_tensor_op
      (LayoutTensorOpReduceSumGeneric ?in_layout_tensor ?reduction_dim ?out_layout))
  )
)

; Int: PROOF-GATED (non-wrapping ruling 2026-08-11) — mints
; only where the value-bounds lattice proves every element in
; width; unproven spellings refuse and the Strict* family is
; the dynamic-checked escape hatch.
; Layout Tensor Op Reduce Sum Generic
(rule
  (
    (= ?in_layout_tensor (LayoutTensorLit ?in_logical ?in_layout))
    ; CANONICAL-ONLY: this slot must carry its logical's own right-major
    ; contiguous layout. The kernels fabricate dense right-major strides
    ; in Rust and never read a layout, so before this premise a left-major
    ; operand was accepted and silently mis-indexed.
    (= ?in_layout (RightMajorContiguousElementLayoutLit ?in_cshape ?in_cbits))
    (= ?in_cshape (shape-of ?in_logical))
    (= ?in_cbits (bits-of (dtype-of ?in_logical)))
    (= ?out_layout_tensor (LayoutTensorLit ?out_logical ?out_layout))
    ; CANONICAL-ONLY: this slot must carry its logical's own right-major
    ; contiguous layout. The kernels fabricate dense right-major strides
    ; in Rust and never read a layout, so before this premise a left-major
    ; operand was accepted and silently mis-indexed.
    (= ?out_layout (RightMajorContiguousElementLayoutLit ?out_cshape ?out_cbits))
    (= ?out_cshape (shape-of ?out_logical))
    (= ?out_cbits (bits-of (dtype-of ?out_logical)))
    (= ?out_logical (LogicalReduceSum ?in_logical ?reduction_dim))
    (= (dtype-of ?out_logical) (Int))
    (= ?value_lower (value-lower-bound-of ?out_logical))
    (= ?value_upper (value-upper-bound-of ?out_logical))
    (>= ?value_lower (bigint -2147483648))
    (<= ?value_upper (bigint 2147483647))
    (= (injectivity-of ?out_layout_tensor) (Injective))
  )
  (
    (let ?layout_tensor_op
      (LayoutTensorOpLit
        (LayoutTensorCons ?in_layout_tensor (LayoutTensorNil))
        (LayoutTensorCons ?out_layout_tensor (LayoutTensorNil))))
    (union ?layout_tensor_op
      (LayoutTensorOpReduceSumGeneric ?in_layout_tensor ?reduction_dim ?out_layout))
  )
)

; Int64: PROOF-GATED (non-wrapping ruling 2026-08-11) — mints
; only where the value-bounds lattice proves every element in
; width; unproven spellings refuse and the Strict* family is
; the dynamic-checked escape hatch.
; Layout Tensor Op Reduce Sum Generic
(rule
  (
    (= ?in_layout_tensor (LayoutTensorLit ?in_logical ?in_layout))
    ; CANONICAL-ONLY: this slot must carry its logical's own right-major
    ; contiguous layout. The kernels fabricate dense right-major strides
    ; in Rust and never read a layout, so before this premise a left-major
    ; operand was accepted and silently mis-indexed.
    (= ?in_layout (RightMajorContiguousElementLayoutLit ?in_cshape ?in_cbits))
    (= ?in_cshape (shape-of ?in_logical))
    (= ?in_cbits (bits-of (dtype-of ?in_logical)))
    (= ?out_layout_tensor (LayoutTensorLit ?out_logical ?out_layout))
    ; CANONICAL-ONLY: this slot must carry its logical's own right-major
    ; contiguous layout. The kernels fabricate dense right-major strides
    ; in Rust and never read a layout, so before this premise a left-major
    ; operand was accepted and silently mis-indexed.
    (= ?out_layout (RightMajorContiguousElementLayoutLit ?out_cshape ?out_cbits))
    (= ?out_cshape (shape-of ?out_logical))
    (= ?out_cbits (bits-of (dtype-of ?out_logical)))
    (= ?out_logical (LogicalReduceSum ?in_logical ?reduction_dim))
    (= (dtype-of ?out_logical) (Int64))
    (= ?value_lower (value-lower-bound-of ?out_logical))
    (= ?value_upper (value-upper-bound-of ?out_logical))
    (>= ?value_lower (bigint -9223372036854775808))
    (<= ?value_upper (bigint 9223372036854775807))
    (= (injectivity-of ?out_layout_tensor) (Injective))
  )
  (
    (let ?layout_tensor_op
      (LayoutTensorOpLit
        (LayoutTensorCons ?in_layout_tensor (LayoutTensorNil))
        (LayoutTensorCons ?out_layout_tensor (LayoutTensorNil))))
    (union ?layout_tensor_op
      (LayoutTensorOpReduceSumGeneric ?in_layout_tensor ?reduction_dim ?out_layout))
  )
)
