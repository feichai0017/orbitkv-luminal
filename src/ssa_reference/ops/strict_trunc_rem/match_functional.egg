; StrictTruncRemFunctionalGeneric — Int: the dynamic-checked escape hatch, NO proof
; premise (the kernel's checked arithmetic is the contract; overflow /
; zero divisor is a loud kernel error).
(rule
  (
    (= ?in1_layout_tensor (LayoutTensorLit ?in1_logical ?in1_layout))
    (= ?in2_layout_tensor (LayoutTensorLit ?in2_logical ?in2_layout))
    (= ?out_layout_tensor (LayoutTensorLit ?out_logical ?out_layout))
    (= ?out_logical (LogicalStrictTruncRem ?in1_logical ?in2_logical))
    (= (injectivity-of ?out_layout_tensor) (Injective))
    (= (dtype-of ?out_logical) (Int))
  )
  (
    (let ?layout_tensor_op
      (LayoutTensorOpLit
        (LayoutTensorCons ?in1_layout_tensor
          (LayoutTensorCons ?in2_layout_tensor (LayoutTensorNil)))
        (LayoutTensorCons ?out_layout_tensor (LayoutTensorNil))))
    (union ?layout_tensor_op
      (LayoutTensorOpStrictTruncRemFunctionalGeneric ?in1_layout_tensor ?in2_layout_tensor ?out_layout))
  )
)

; StrictTruncRemFunctionalGeneric — Int64: the dynamic-checked escape hatch, NO proof
; premise (the kernel's checked arithmetic is the contract; overflow /
; zero divisor is a loud kernel error).
(rule
  (
    (= ?in1_layout_tensor (LayoutTensorLit ?in1_logical ?in1_layout))
    (= ?in2_layout_tensor (LayoutTensorLit ?in2_logical ?in2_layout))
    (= ?out_layout_tensor (LayoutTensorLit ?out_logical ?out_layout))
    (= ?out_logical (LogicalStrictTruncRem ?in1_logical ?in2_logical))
    (= (injectivity-of ?out_layout_tensor) (Injective))
    (= (dtype-of ?out_logical) (Int64))
  )
  (
    (let ?layout_tensor_op
      (LayoutTensorOpLit
        (LayoutTensorCons ?in1_layout_tensor
          (LayoutTensorCons ?in2_layout_tensor (LayoutTensorNil)))
        (LayoutTensorCons ?out_layout_tensor (LayoutTensorNil))))
    (union ?layout_tensor_op
      (LayoutTensorOpStrictTruncRemFunctionalGeneric ?in1_layout_tensor ?in2_layout_tensor ?out_layout))
  )
)
